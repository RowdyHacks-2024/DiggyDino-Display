import React from 'react';
import Thread from './Thread';

const Feed = () => {
    return (
        <article className="resource-container">
            <Thread />
        </article>
    );
};

export default Feed;